apiVersion: v1
kind: PersistentVolume
metadata:
  name: gitlab-gitlab-ce-etc
  namespace: gitlab
spec:
  capacity:
    storage: 16Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /home/supermap/gitlab/gitlab-gitlab-ce-etc

